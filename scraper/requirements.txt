# Runtime dependencies for the pilot cadet monitor.
#
# Pinned to compatible-release ranges: the scheduled GitHub Actions job must stay
# reproducible, but patch/minor security fixes for these libraries should flow in
# without a manual edit.
#
#   httpx            -- fast-path HTTP/2-capable client for static career pages
#   beautifulsoup4   -- DOM traversal for selector-driven extraction
#   lxml             -- fast parser + the only backend that supports XPath selectors
#   playwright       -- headless Chromium for Workday / SuccessFactors / Taleo portals
#
httpx>=0.27,<0.29
beautifulsoup4>=4.12,<5
lxml>=5.2,<7
playwright>=1.44,<2

# Development / verification only (installed by CI in a separate step).
pytest>=8.0,<9
